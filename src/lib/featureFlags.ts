/**
 * When false, post-apply flow skips `/interview/...` and the interview page does not
 * call `startAiInterview` / show ChatUI. Re-enable when the AI interview is ready.
 */
export const AI_INTERVIEW_ENABLED = false;
